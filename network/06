
The full path of the resolv.conf file is:
		/etc/resolv.conf

The resolve.conf on this machine is somewhat basic and only contain the DNS ip and the domain name,
as well as a short command. This file is usually used by the resolver routine.

